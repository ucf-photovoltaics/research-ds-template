# Pinned requirements compiled from requirements.in.
#
# To regenerate (requires Python >=3.10):
#   pip install pip-tools
#   pip-compile --generate-hashes requirements.in -o requirements.txt
#
# CI recompiles this for the exact CI platform (see .github/workflows/ci.yml).
# This committed file documents pinned floor versions for reference and for
# local installs on supported Python versions (>=3.10).
#
# SECURITY NOTE: Run `pip-audit --requirement requirements.txt` regularly.
#
# Known unfixable findings (update when fixed upstream):
#   None at this time.

pydantic==2.7.1
pyyaml==6.0.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.2
scipy==1.13.1
pillow==12.2.0
boto3==1.34.84
s3fs==2024.3.1
aiohttp==3.14.1
urllib3==2.7.0
